{% extends "DodiciFansworldWebBundle:User:base.html.twig" %}

{% block title %}{{ parent() }} - Información{% endblock %}

{% block profile_tabs %}
	{% set active_tab = 'detail' %}
	{{ parent() }}
{% endblock %}



{% block profile_body %}
    {% embed "DodiciFansworldWebBundle:Layout/Embed:profile_inner_lsidebar.html.twig" %}
        {% block inner_sidebar %}
                {% if user == app.user %}
                    {% set userLogged = true %}
                {% else %}
                    {% set userLogged = false %}
                {% endif %}
                {% include "DodiciFansworldWebBundle:Default:blocks/info_list.html.twig" with { entity:user, personalData: personalData, showEdit: userLogged, list_title: 'Tus Datos Personales:', list_type: 'user' } %}
        {% endblock %}
        
        {% block inner_content %}
                {% if user.content %}
                    {% include "DodiciFansworldWebBundle:Default:blocks/text_box.html.twig" with { box_title: 'Sobre mi:', box_content: user.content, box_class: 'aboutMe' } %}
                {% endif %}
                {#
                <section id="myInterests">
                    <dl>
                        {% for interest in interests %}
                            {% if interest.interest is defined %}
                                <dt class="dato">{{ interest.category }}</dt>
                                        {% for ele in interest.interest %}
                                            <dd data-idinterest="1">
                                                <img alt="" src="{{ele.image}}"> 
                                                <span class="name">{{ele.title}}</span>
                                            </dd>
                                        {% endfor %}
                            {% endif %}
                        {% endfor %}
                    </dl>
                </section>
                #}
        {% endblock %}
    {% endembed %}		
{% endblock %}