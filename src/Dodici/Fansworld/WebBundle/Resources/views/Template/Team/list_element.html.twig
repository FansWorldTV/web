{% raw %}
<dt class="team">
    <img src="{{>image}}" />
    <p>
        <a href="{{>url}}" class="name">{{>title}}</a>
        <br/>
        {{if idols}}
        <span class="idols">Idolos: 
            {{for idols}}
                <a href="{{>url}}">{{>name}}</a>, 
            {{/for}}
        </span>
        {{/if}}
    </p>
</dt>
    <dd>
        <button class="btn btn-success {{if teamship}}disabled{{else}}btn_teamship add{{/if}}" data-team-id="{{>id}}">Ser fan</button>
        
        <ul>
                <li><a href="{{>fansUrl}}"><span>{{>fanCount}}</span><br/> fans</a></li>
                <li><a href="{{>videosUrl}}"><span>{{>videoCount}}</span><br/> videos</a></li>
                <li><a href="{{>photosUrl}}"><span>{{>photoCount}}</span><br/> fotos</a></li>
            
        </ul>
    </dd>
{% endraw %}