{% extends 'DodiciFansworldWebBundle:Comment:comment.base.html.twig' %}

{% block avatar %}
	<div class="ico">
		<img src="{{ asset('bundles/dodicifansworldweb/images/ico/user_add.png' )}}" alt="icon" />
	</div>
{% endblock %}

{% block action %}
	<span class="action_user">
		<a href="{{ path('user_land', {username: comment.target.username }) }}">{{ comment.target }}</a> 
		{{ 'and'|trans }} 
		<a href="{{ path('user_land', {username: comment.author.username }) }}">{{ comment.author }}</a> 
		{{ 'are now friends'|trans }}
	</span>
{% endblock %}

{% block title %}{% endblock %}
{% block description %}{% endblock %}
