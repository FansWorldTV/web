{% extends "DodiciFansworldWebBundle::web.html.twig" %}

{% block title %}{{ parent() }} - Contest{% endblock %}

{% block content %}
<div class="col_468 left margin contests">
    <div class="cont clearfix">
        <div class="nota_ampliada">

            <div class="media">
            	<h2{% if contest.image is null %} class="noimage"{% endif %}>{{ contest.title }}</h2>
            	{% if contest.image %}
            		{% thumbnail contest.image, 'big' with {'class': 'articleimg'} %}
            	{% endif %}
            	{% include 'DodiciFansworldWebBundle:Default:share.html.twig' with {entity: contest} %}
            </div>
            <div class="hidden" id="formParticipate">
                <form action="" class="formParticipate">
                    <textarea name="content"></textarea>
                    <input type="submit" value="Participar" name="submit"/>
                </form>
            </div>
            <div class="contenido">
                {{ contest.content|raw() }}
                {% if isParticipant %}
                    <div class="options">Ya estas participando</div>
                {% else %}
                    <div class="options"><a class="btn_action2 contestParticipate" contestId="{{ contest.id }}" contestType="{{ contest.type }}" href="#1">Participar</a></div>
                {% endif %}
            </div>
        </div>
    </div>
    <div id="participateSplash" class="hidden">
        <div class="2">
            <textarea></textarea>
            <input type="button" value="Enviar"/>
        </div>
    </div>
    <div class="cont clearfix">
        <h3>Comentarios</h3>
        
		{% include 'DodiciFansworldWebBundle:Comment:comment_block.html.twig' with {entity: contest} %}
    </div>
    <div class="cont clearfix">
        <h3>Agregar comentario</h3>
        <div class="add_comment">
            <form>
                <textarea cols="" rows="4"></textarea>
                <input type="hidden" class="contestId" value="{{ contest.id }}" />
                <a class="btn" href="#1">Comentar</a>
            </form>
        </div>
    </div>
    <script type="text/javascript">
            $(document).ready(function(){
                contest.init();
            });
    </script>
</div>
<div id="templates" class="contest hidden">
    <div class="comment clearfix">
        <div class="avatar">
            <a href="#1">
                <img src="{{ asset('bundles/dodicifansworldweb/images/user_pic.jpg' )}}" width="50" height="50" />
            </a>
        </div>
        <div class="user_comment">
            <span class="action_user" >
                <a href=""></a>
                <span></span>
            </span>
        </div>
    </div>
</div>
{% endblock %}