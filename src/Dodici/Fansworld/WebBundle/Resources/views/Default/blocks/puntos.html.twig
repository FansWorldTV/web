{% if score > 0 %}
    {% if score >= 1000 %}
        {% set mscore = (score / 1000) %}
        {{ mscore }}k
    {% else %}
        {{ score }}
    {% endif %}
{% else %}
    {{ score }}
{% endif %}