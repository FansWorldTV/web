{% if appstate.mobile %}
	{% include [
	_self.templatename|replace({'.html.twig' : '.mobile.html.twig'}),
	_self.templatename|replace({'.html.twig' : '.web.html.twig'})
	] %}
{% else %}
	{% include [
	_self.templatename|replace({'.html.twig' : '.web.html.twig'})
	] %}
{% endif %}