
{% block field_label %}
{% spaceless %}
    <label for="{{ id }}">
    	{{ id|trans([], 'FOSUserBundle') }}
    	{% if required %}
    		<span class="required" title="Campo requerido">*</span>
    	{% endif %}
    </label>
{% endspaceless %}
{% endblock field_label %}